# SyncNews Audio 変換ワーカー 環境変数
# 共有の claude_test/.env を先に読み、この backend/.env で上書きする（override=True）。

# OpenAI（本文抽出・翻訳：GPT-4o）
OPENAI_API_KEY=

# ElevenLabs（TTS + 文字タイムスタンプ）
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_JA=
ELEVENLABS_VOICE_EN=

# Supabase（サーバ専用キー：絶対にクライアントへ出さない）
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
