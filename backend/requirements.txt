fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1

# AI providers
openai==1.59.6            # GPT-4o（本文抽出・翻訳）

# HTTP（ElevenLabs with-timestamps の直叩き）
# 0.28系は supabase 2.10 と非互換。openai/supabase 双方が許す 0.27系に固定。
httpx==0.27.2

# Supabase（Postgres tracks/segments + Storage 音声アップロード）
supabase==2.10.0

# 金庫スキーマ(syncnews_vault)への直接Postgres接続（PostgREST不可のため）。
# Supabase Session pooler 経由。DATABASE_URL を使用。
asyncpg==0.31.0

# 日本語の漢字読み精度向上（旧: 合成前にカナ読みへ変換）。
# ※日本語TTSは Azure に移行したため現状は未使用。英語経路や将来用に残置。
sudachipy==0.6.11
sudachidict-core==20260428

# 日本語TTS（Azure Speech）。漢字をネイティブに正しく読み（数字/助数詞/拗音も）、
# SSMLブックマークで文単位タイムスタンプを取得して同期に使う。F0無料枠で運用。
azure-cognitiveservices-speech==1.50.0
